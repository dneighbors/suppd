<%= form_with(model: supplement) do |form| %>
  <% if supplement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplement.errors.count, "error") %> prohibited this supplement from being saved:</h2>

      <ul>
        <% supplement.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vendor_id %>
    <%= form.number_field :vendor_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
